import 'package:flutter/material.dart';

class UpdateItem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('UpdateItem'),
      ),
      body: Container(
        
      ),
    );
  }
}